# servers-vm

The `server` brick's VM: a Debian cloud image, cloud-init for the address
and the key, no GPU, resized by `cores`/`memory` as games are added; a
second instance from the same module when one is full. **Lands at G3** —
the variables are the contract, the resource follows the first named game.
