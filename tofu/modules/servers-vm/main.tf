# servers-vm — the dedicated-servers VM (Debian cloud image + cloud-init, no
# GPU, a podman container per game inside). The contract is the variables
# file; the resource lands at G3 with the first named game.
