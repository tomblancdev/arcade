# doorman-ct

The doorman's container: unprivileged Debian CT, one NIC on a named bridge
with the hypervisor firewall on, `features` left to the role (nesting +
keyctl for podman are root@pam-only in the PVE API). **Lands at G2.**
