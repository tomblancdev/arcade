# The collection `tomblancdev.arcade` — the platform's roles, shipped from
# the repo's ansible/ directory. A lab pins it by tag:
#   collections:
#     - name: https://github.com/tomblancdev/arcade.git#/ansible/
#       type: git
#       version: v0.2.0
namespace: tomblancdev
name: arcade
version: 0.6.3
readme: README.md
authors:
  - Tom Blanc <dev@tomblanc.fr>   # no-environment: ok — the author, not a network
description: "L'Arcade — a platform of bricks for a home games console and dedicated game servers on Proxmox: a GPU-owner VM with glass (screen) or without (appliance), the seat engine (one container per person, on demand), emulation + streaming, the library, a container per game server"
license:
  - MIT
tags:
  - proxmox
  - gaming
  - sunshine
  - moonlight
  - retrodeck
  - bazzite
  - wolf
  - streaming
  - homelab
repository: https://github.com/tomblancdev/arcade
documentation: https://github.com/tomblancdev/arcade/tree/main/ansible
homepage: https://github.com/tomblancdev/arcade
issues: https://github.com/tomblancdev/arcade/issues
build_ignore:
  - "*.tar.gz"
