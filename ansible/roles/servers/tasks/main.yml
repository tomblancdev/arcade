---
# servers — one podman Quadlet per dedicated game server, limits, worlds export
# Lands at G3 (the platform's build plan). The contract is in defaults/main.yml;
# installing the collection before the role exists is the point: the pin and
# the data land first, the tasks fill in at their gate.
