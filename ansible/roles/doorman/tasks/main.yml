---
# doorman — the doorman app as a podman Quadlet: config, data dir, the wake/start/stop key
# Lands at G2 (the platform's build plan). The contract is in defaults/main.yml;
# installing the collection before the role exists is the point: the pin and
# the data land first, the tasks fill in at their gate.
