---
# screen — the GPU-owner VM after its appliance install: user, address, mounts, idle timer, shipper
# Lands at G1 (the platform's build plan). The contract is in defaults/main.yml;
# installing the collection before the role exists is the point: the pin and
# the data land first, the tasks fill in at their gate.
